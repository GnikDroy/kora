import "std/libc";

void sleep(ms: int) {
    let left = ms;
    while (left > 0) {
        let chunk = left;
        if (chunk > 500) {
            chunk = 500;
        }
        libc.usleep(chunk * 1000);
        left = left - chunk;
    }
}
