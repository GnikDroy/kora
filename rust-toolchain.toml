# Pinned to nightly: the crate still uses `#![feature(stmt_expr_attributes)]`
# for the `#[rustfmt::skip]` attributes on match blocks. Everything else now
# builds on edition 2024. Removing those 5 attributes would allow moving to stable.
[toolchain]
channel = "nightly"
